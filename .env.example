# Set automatically by Vercel on every deployment; do not set it yourself.
# Locally, it's absent and the app falls back to a "local-dev" marker
# (see lib/deployed-commit.ts).
VERCEL_GIT_COMMIT_SHA=
